<Window x:Class="CinemaManagement.UI.Views.SpectatorDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cinema - Book Your Tickets" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#0d1117">

    <Window.Resources>
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="#21262d"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30363d"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#161b22" Padding="20">
            <StackPanel>
                <TextBlock Text="🎬 CINEMA" FontSize="22" FontWeight="Bold" 
                          Foreground="#58a6ff" Margin="0,0,0,5"/>
                <TextBlock x:Name="WelcomeText" Text="Welcome!" 
                          Foreground="#8b949e" Margin="0,0,0,30"/>

                <Button Content="🎥 Browse Movies" Click="BrowseMovies_Click"
                       Style="{StaticResource SidebarButton}" Margin="0,0,0,10"/>
                <Button Content="🎫 My Reservations" Click="MyReservations_Click"
                       Style="{StaticResource SidebarButton}" Margin="0,0,0,30"/>
                <Button Content="🚪 Logout" Click="Logout_Click"
                       Style="{StaticResource SidebarButton}" Background="#991b1b"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Now Showing" FontSize="28" FontWeight="Bold" 
                          Foreground="White" Margin="0,0,0,20"/>

                <!-- Search Box -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBox" Grid.Column="0" 
                            Tag="Search movies..." 
                            Background="#161b22" Foreground="White"
                            Padding="12" FontSize="14" BorderThickness="0"/>
                    <Button Grid.Column="1" Content="🔍 Search" Click="Search_Click"
                           Style="{StaticResource ActionButton}" Margin="10,0,0,0"/>
                </Grid>

                <!-- Movies List -->
                <ItemsControl x:Name="MoviesItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#161b22" CornerRadius="8" 
                                   Padding="20" Margin="0,0,0,15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Title}" FontSize="20" 
                                                  FontWeight="Bold" Foreground="White"/>
                                        <TextBlock FontSize="14" Foreground="#8b949e" Margin="0,5,0,0">
                                            <Run Text="Genre:"/>
                                            <Run Text="{Binding Genre}"/>
                                            <Run Text=" | Duration:"/>
                                            <Run Text="{Binding DurationMinutes}"/>
                                            <Run Text="min | Rating:"/>
                                            <Run Text="{Binding Rating}"/>
                                            <Run Text="/5"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="📅 View Showtimes" 
                                           Tag="{Binding Id}"
                                           Click="ViewShowtimes_Click"
                                           Style="{StaticResource ActionButton}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>